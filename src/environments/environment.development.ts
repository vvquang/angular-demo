const scheme = 'https://';
const host   = 'jsonplaceholder.typicode.com';
const port   = '';
const path   = '';

const baseUrl = scheme + host + port + path;

export const environment = {
  production      : false,
  appName         : 'Pet',
  apiBaseUrl      : baseUrl,
};
